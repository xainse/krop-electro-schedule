# Changelog

Всі важливі зміни в проєкті будуть документуватися в цьому файлі.

Формат заснований на [Keep a Changelog](https://keepachangelog.com/uk/1.0.0/),
і цей проєкт дотримується [Semantic Versioning](https://semver.org/lang/uk/).

## [2.3] - 2026-01-22

### Видалено
- **RSS парсер (`api/rss_fetcher.php`)**: Видалено модуль отримання даних через RSS фід
  - Функції `fetchFromRSS()`, `parseRSSItem()`, `extractTextFromHTML()`, `fetchRSSContent()`
  - Документ `docs/RSS_MIGRATION_PLAN_2026-01-22.md`
- **RSS функції в data.php**: Видалено `saveRSSMessage()` та `getRSSMessages()`
- **RSS константи в config.php**: 
  - `RSS_URL`
  - `RSS_CHECK_INTERVAL`
  - `RSS_MESSAGES_FILE`
  - `LAST_RSS_CHECK_FILE`
  - `SOURCE_RSS`

### Змінено
- **Архітектура fallback в blackout.php**: 
  - Було: Telegram → RSS → Site
  - Стало: Telegram → Site
- **Функції перевірки джерел**:
  - `shouldCheckRSS()` перейменовано на `shouldCheckSources()`
  - `saveRSSCheckTimestamp()` перейменовано на `saveSourceCheckTimestamp()`
  - Файл `last_rss_check.txt` замінено на `last_source_check.txt`
- **Документація**: 
  - Оновлено README.md - видалено згадки RSS
  - Видалено розділ "План міграції на RSS" з документації

### Причина
- Спрощення архітектури проєкту
- RSS фід був проміжним рівнем між Telegram і Site парсингом
- Telegram Web Scraper надійніший і достатній як primary джерело

## [2.2] - 2026-01-22

### Додано
- **Статистика годин на добу**: Додано відображення загальної кількості годин світла та відключення
  - Інформація відображається прямо в легенді біля позначень "Електрика є ⚡" та "Відключено 🌑"
  - Формат відображення: "H:MM" (години:хвилини), наприклад "8:30" або "12:00"
  - Враховуються півгодинні періоди (кожен по 30 хвилин)
  - Автоматичний підрахунок при кожному оновленні графіку
  - Нові JavaScript функції:
    - `calculateDailyStats(periods)` - підрахунок статистики з масиву 48 півгодинних періодів
    - `formatHoursText(hours, minutes)` - форматування часу у компактному вигляді
  - Додано CSS стилі для `.hours-stat` (приглушений колір, менший розмір шрифту)

### Покращення UX
- Користувачі тепер можуть одразу побачити загальну кількість годин зі світлом та без на весь день
- Компактний формат часу не перевантажує інтерфейс

## [2.1] - 2026-01-22

### 🚀 Додано
- **Telegram Web Scraper (`api/telegram_fetcher.php`)**: Новий модуль для парсингу веб-версії Telegram каналу
  - Пряме отримання даних з https://t.me/s/SvitloKropyvnytskyiMisto
  - Інкрементальне оновлення: при першому запуску завантажує останні 10 повідомлень, потім тільки нові
  - Парсинг HTML через DOMDocument та DOMXPath
  - Збереження історії повідомлень в `cache/telegram_messages.json`
  - Підтримка UTF-8, правильна обробка українських символів
  - Обхід базового захисту через правильні User-Agent та Accept headers
  - Автоматичне виявлення ГАВ/СГАВ з повідомлень

- **Нові функції в data.php**:
  - `saveTelegramMessage($messageData)` - збереження Telegram повідомлень
  - `getTelegramMessages($limit)` - отримання історії повідомлень
  - `getLastTelegramId()` - отримання ID останнього обробленого повідомлення
  - `saveLastTelegramId($id)` - збереження ID для інкрементального оновлення

### Змінено
- **Пріоритет джерел даних в blackout.php**: 
  1. Telegram Web Scraper (primary)
  2. RSS Fetcher (fallback)
  3. kiroe.com.ua (останній fallback)
- **Конфігурація (`config.php`)**: додано константи для Telegram
  - `SOURCE_TELEGRAM` - ідентифікатор джерела
  - `TELEGRAM_CHANNEL_URL` - URL веб-версії каналу
  - `TELEGRAM_MESSAGES_FILE` - шлях до файлу з історією
  - `TELEGRAM_CHECK_INTERVAL` - інтервал перевірки (5 хвилин)

### Переваги нового підходу
- **Швидкість**: Прямий доступ до веб-версії каналу без проміжних сервісів
- **Надійність**: Не залежить від RSS.app або інших сторонніх сервісів
- **Актуальність**: Отримання даних безпосередньо з джерела
- **Інкрементальність**: Обробка тільки нових повідомлень після першого запуску
- **Зворотна сумісність**: RSS залишається як fallback

## [2.0.1] - 2026-01-22

### Змінено
- **RSS URL**: Замінено на https://rss.app/feeds/mqBOeTuLJO2YFCZC.xml (більш актуальні дані)
- **Підтримка СГАВ**: Додано розпізнавання "спеціальний графік аварійних відключень" (СГАВ)
  - Оновлено `parser.php` - додано патерни для СГАВ
  - Оновлено `site_fetcher.php` - підтримка СГАВ при парсингу HTML
  - Оновлено `blackout.php` - розпізнавання СГАВ
  - СГАВ прирівнюється до ГАВ і відображається з тим самим попередженням

## [2.0] - 2026-01-22

### 🚀 MAJOR CHANGE: Перехід з Telegram Bot API на RSS

**Причина зміни:** Спрощення архітектури, підвищення надійності та швидкості отримання даних

### Додано
- **RSS Fetcher (`api/rss_fetcher.php`)**: Новий модуль для отримання даних через RSS фід
  - `fetchFromRSS($limit)` - завантаження та парсинг RSS
  - `parseRSSItem($item)` - обробка окремого RSS елемента
  - `extractTextFromHTML($html)` - витягування тексту з HTML description
  - Підтримка автоматичного парсингу всіх типів повідомлень (графіки, ГАВ, зміни)
- **RSS історія**: Збереження всіх отриманих повідомлень в `cache/rss_messages.json`
- **Інтервал перевірки**: Автоматична перевірка RSS кожні 5 хвилин
- **Функції в data.php**:
  - `saveRSSMessage()` - збереження RSS повідомлення в історію
  - `getRSSMessages()` - читання історії RSS повідомлень
- **RSS конфігурація**:
  - `RSS_URL` - посилання на RSS фід
  - `RSS_CHECK_INTERVAL` - інтервал перевірки (5 хвилин)
  - `RSS_MESSAGES_FILE` - файл історії RSS
  - `LAST_RSS_CHECK_FILE` - timestamp останньої перевірки

### Змінено
- **Каскадний fallback**: RSS → Site (замість Telegram → Site)
  1. Спочатку перевіряється RSS фід
  2. Якщо RSS недоступний → fallback на парсинг kiroe.com.ua
  3. Якщо обидва недоступні → використання кешу
- **API endpoint (`api/blackout.php`)**:
  - Додано `shouldCheckRSS()` - перевірка 5-хв інтервалу
  - Додано `saveRSSCheckTimestamp()` - збереження часу перевірки
  - Оновлена логіка оновлення даних з RSS fallback
  - При ініціалізації (порожній кеш) завантажується 100 останніх RSS повідомлень
  - При поточній роботі - 10 останніх повідомлень
- **Конфігурація (`api/config.php`)**:
  - Видалено всі Telegram константи (TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, SETUP_PASSWORD)
  - Додано RSS константи
  - SOURCE_TELEGRAM замінено на SOURCE_RSS
- **Документація**:
  - Оновлено README.md з інформацією про RSS
  - Додано план міграції `docs/RSS_MIGRATION_PLAN_2026-01-22.md`
  - Видалено застарілі Telegram інструкції

### Видалено
- ❌ `api/telegram_fetcher.php` - Telegram Bot API fetcher
- ❌ `api/telegram_setup.php` - веб-інтерфейс налаштування бота
- ❌ `api/telegram_webhook.php` - Telegram webhook handler
- ❌ `docs/TELEGRAM_BOT_PLAN.md` - план Telegram інтеграції
- ❌ `docs/TELEGRAM_SETUP_INSTRUCTIONS.md` - інструкції налаштування бота
- ❌ `docs/QUICK_START.md` - швидкий старт для Telegram
- ❌ `docs/НАСТУПНІ_КРОКИ.md` - наступні кроки (Telegram-специфічні)

### Архітектура (нова)
```
RSS Фід (кожні 5 хв) → JSON кеш → API → Frontend
        ↓ (fallback)
  kiroe.com.ua сайт
```

**Переваги нового підходу:**
- ✅ Простота - не потрібен Bot Token, webhook, налаштування
- ✅ Надійність - RSS більш стабільний ніж Bot API
- ✅ Швидкість - прямий доступ до даних без проміжних запитів
- ✅ Fallback - автоматичне перемикання на сайт при недоступності RSS
- ✅ Історія - збереження всіх отриманих повідомлень

### Міграція
Для існуючих інсталяцій:
1. Оновити `api/config.php` (видалити Telegram токени, не критично якщо залишаться)
2. Переконатись що доступний RSS URL: https://fetchrss.com/feed/1viogN2iQDkR1viofe38pFiF.rss
3. API автоматично почне використовувати RSS при наступному запиті

## [1.36] - 2025-01-20

### Додано
- **Папка документації**: Вся документація (CHANGELOG, інструкції, плани) перемішена в `docs/`
- **Telegram Bot інтеграція (підготовка)**: Створено інфраструктуру для автоматичного отримання графіків з Telegram каналу
- **Документація**: Створено детальні інструкції та плани
- `api/config.example.php` - шаблон конфігурації з параметрами Telegram бота
- `api/data.php` - модуль для роботи з JSON кешем (schedules.json, telegram_messages.json)
- `api/parser.php` - парсер повідомлень про графіки (підтримка Telegram та HTML форматів)
- `api/telegram_webhook.php` - webhook handler для отримання повідомлень від Telegram Bot API
- `api/telegram_fetcher.php` - модуль для ручного завантаження повідомлень через getUpdates
- `api/site_fetcher.php` - fallback парсер для kiroe.com.ua (перенесена логіка з blackout.php)
- `api/telegram_setup.php` - веб-інтерфейс для налаштування бота через браузер
- `api/blackout_new.php` - новий API endpoint з каскадним fallback (JSON кеш → Telegram → Сайт)
- `api/cache/.htaccess` - захист JSON файлів від прямого доступу
- `TELEGRAM_BOT_PLAN.md` - повна документація архітектури системи
- `TELEGRAM_SETUP_INSTRUCTIONS.md` - детальна покрокова інструкція налаштування
- `QUICK_START.md` - швидкий старт для налаштування

### Змінено
- Оновлено `api/.htaccess`: додано правила безпеки для захисту config.php та службових модулів
- Оновлено `.gitignore`: додано `api/config.php` (містить секретні токени)

### Архітектура
- **Каскадний fallback**: JSON кеш (швидко) → Telegram (автоматично) → Сайт (резерв)
- **Автоматичність**: Webhook отримує нові повідомлення в реальному часі
- **Надійність**: Три рівні захисту від недоступності джерел даних
- **Безпека**: Захист конфігурації, rate limiting, валідація даних
- **Модульність**: Кожен компонент в окремому файлі
- **Зворотна сумісність**: Старий API (blackout.php) залишається робочим

### Примітки
- Фронтенд (index.html) поки не змінено - використовує старий API
- Telegram інтеграція працює паралельно зі старим рішенням
- Переключення на новий API буде після тестування (версія 2.0)

## [1.35] - 2025-01-10

### Змінено
- Оновлено стилі виділення активної години: верхній та нижній бордери тепер використовують стандартний колір рамки таблиці замість повного видалення

## [1.34] - 2025-01-10

### Виправлено
- Уточнено стилі виділення активної години: явно встановлено border-top та border-bottom в 0

## [1.33] - 2025-01-10

### Видалено
- Видалено таблицю з емоджі - залишено тільки основну таблицю з кольоровими ячейками

### Змінено
- Оновлено виділення активної години: прибрано верхній та нижній бордери, залишено тільки бокові (лівий та правий)

## [1.32] - 2025-01-10

### Додано
- Таблиця з емоджі зверху основної таблиці: відображення графіків відключень через емоджі замість кольорових ячеек
- 🟨 (жовтий квадрат) для стану "є електрика", ⬛ (чорний квадрат) для "відключено"
- Номери черг розміром 9px перед кожним рядком
- Автоматичне виділення активної години в таблиці з емоджі

## [1.31] - 2025-01-10

### Змінено
- Змінено орієнтацію таблиці всіх черг: черги тепер по вертикалі (ліворуч), години по горизонталі
- Прибрано заголовки часу з таблиці - виводяться тільки заголовки черг
- Додано виділення стовпчика з активною годиною (синя рамка)
- Зменшено розмір ячеек на 30% для кращої компактності (28px замість 40px)

## [1.30] - 2025-01-10

### Додано
- Таблиця всіх черг нижче банера донації: візуалізація графіків відключень для всіх 12 черг одночасно
- По горизонталі відображаються черги (1.1-6.2), по вертикалі - години (0-23)
- Квадратні ячейки без тексту, зафарбовані тими ж кольорами що і на головному екрані
- Підтримка півгодинних періодів у таблиці (градієнти для змішаних станів)
- API endpoint `?all=1` для отримання даних всіх черг одночасно
- Автоматичне оновлення таблиці при оновленні даних з серверу

### Змінено
- API тепер підтримує параметр `all=1` для повернення даних всіх черг з кешу

## [1.29] - 2025-01-10

### Додано
- Перевірка на скасування ГАВ: парсер тепер виявляє текст "дію графіка аварійних відключень (ГАВ) скасовано" і автоматично встановлює `emergency_mode = false`
- Підтримка використання кешу при ГАВ: якщо графіки не оновлені на сайті, використовуються графіки з кешу

### Виправлено
- Кеш більше не видаляється, поки не отримані нові дані з сайту
- При недоступності сайту (kiroe.com.ua не працює) API використовує дані з кешу замість повернення помилки
- Фронтенд завжди отримує дані (з кешу або з сайту), навіть якщо сайт недоступний

### Змінено
- Покращено логіку кешування: кеш завжди завантажується якщо він існує, незалежно від TTL
- Оптимізовано обробку помилок: при будь-якій помилці парсингу використовується кеш якщо він доступний

## [1.28] - 2025-11-20

### Змінено
- Покращено функціонал відображення ГАВ: замість повідомлення "час невідомий" тепер показується останній плановий графік для орієнтації
- Змінено текст попередження про ГАВ на більш інформативний: "Діє графік аварійних відключень (ГАВ). Нижче показано останній плановий графік для орієнтації, але реальний час відключень може відрізнятися."
- Графік тепер завжди відображається навіть при активному ГАВ, надаючи користувачам орієнтовну інформацію

## [1.27] - 2025-11-20

### Додано
- Підтримка розпізнавання та відображення стану "Графік аварійних відключень (ГАВ)"
- Парсер тепер виявляє повідомлення про ГАВ на сайті kiroe.com.ua
- API тепер повертає поле `emergency_mode` для індикації активного ГАВ
- Відображення попередження про ГАВ з окремим емоджі ⚠️ на фронтенді
- Повідомлення про ГАВ показується в окремому блоці з помаранчевим фоном

### Змінено
- Оновлено структуру кешу для збереження стану ГАВ
- Покращено обробку стану аварійних відключень на фронтенді

## [1.26] - 2025-11-17

### Змінено
- Спрощено відображення застарілих даних: прибрано окремий рядок з повідомленням про застарілі дані
- Тепер інформація про застарілі дані відображається тільки в рядку "Останнє оновлення: хх:хх (⚠️ застарілі)"
- Покращено UX: менше тексту, більше простору на сторінці

## [1.25] - 2025-11-17

### Покращено
- Додано відображення віку застарілих даних (скільки хвилин тому було останнє оновлення)
- Покращено обробку помилок парсингу JSON - тепер також показуються старі дані при помилці
- Покращено повідомлення про застарілі дані - додано детальну інформацію про час останнього оновлення

## [1.24] - 2025-11-17

### Додано
- При помилці завантаження даних з API тепер відображаються останні успішно завантажені дані замість невідомих значень
- Червоне повідомлення про застарілі дані з часом останнього успішного оновлення
- Збереження останніх успішних даних в localStorage для використання при перезавантаженні сторінки
- Відображення збережених даних при першому завантаженні сторінки

### Змінено
- При помилці API дані не скидаються до невідомих значень, а залишаються останні доступні
- Текст "Останнє оновлення" стає червоним при відображенні застарілих даних

## [1.23] - 2025-11-17

### Змінено
- Видалено використання CORS proxy (corsproxy.io) - тепер API викликається напряму
- Додано CORS заголовки в PHP API для дозволу прямого виклику з браузера
- Спрощено код завантаження даних - прибрано функцію `fetchWithProxy()`

## [1.22] - 2025-11-14

### Додано
- Система логування всіх запитів до API в файли `api/logs/blackout_YYYY-MM-DD.log`
- Відстеження швидкості відповіді (час виконання в мілісекундах)
- Відстеження джерела даних (кеш або сайт) для кожного запиту
- Логування IP адреси клієнта та User-Agent
- Автоматична ротація логів (один файл на день)

### Змінено
- Додано функції для логування: `getLogPath()`, `getClientIp()`, `logRequest()`
- Всі запити (успішні та помилки) тепер логуються в JSON форматі

## [1.21] - 2025-11-14

### Додано
- Система кешування даних на 10 хвилин у файлі `api/cache/blackout_cache.json`
- Автоматичне створення папки cache та файлу кешу
- Парсинг всіх черг з HTML при оновленні кешу для зменшення навантаження на джерело

### Змінено
- Оптимізовано роботу API: тепер дані завантажуються з сайту тільки якщо кеш застарів або відсутній
- Додано функції для роботи з кешем: `getCachePath()`, `isCacheValid()`, `loadCache()`, `saveCache()`, `parseAllQueues()`

## [1.20] - 2025-11-14

### Змінено
- Покращено стиль обводки для емоджі: замінено складну обводку на простіший blur ефект (text-shadow: 0 0 6px rgba(255, 255, 255, 0.4))

## [1.19] - 2025-11-14

### Додано
- Тінь для білого тексту часу на темному фоні (text-shadow з прозорістю 25%)
- Біла обводка для емоджі (місяць та блискавка) з прозорістю 15% для кращої видимості

### Змінено
- Спрощено текст про джерело даних - прибрано інформацію про API парсер

## [1.18] - 2025-11-14

### Змінено
- Оновлено текст про джерело даних: тепер вказується що дані беруться зі сторінки kiroe.com.ua через API парсер
- Елемент для виведення помилок API налаштовано для відображення помилок з відповіді API

## [1.17] - 2025-11-14

### Додано
- Підтримка півгодинних періодів відключення електрики
- Відображення півгодинних періодів через CSS градієнти (половина клітинки одним кольором, половина іншим)
- Парсер для формату "HH:MM-HH:MM" з півгодинною точністю

### Змінено
- Змінено API URL на https://xain.in.ua/api/blackout.php?queue=X.X
- Оновлено структуру даних: тепер використовується масив з 48 елементів (півгодинні періоди) замість 24
- Оновлено функцію рендерингу для відображення півгодинних періодів

## [1.16] - 2025-11-12

### Додано
- Додано червоне повідомлення під рядком "Останнє оновлення": "Вибачте за незручності, є проблеми з оновленням даних"

## [1.15] - 2025-11-10

### Змінено
- Оновлено посилання "Версія" у футері - тепер веде на CHANGELOG.md в GitHub репозиторії

## [1.14] - 2025-11-10

### Виправлено
- Виправлено функцію автооновлення даних - тепер дані оновлюються автоматично кожні 10 хвилин

## [1.13] - 2025-10-11

### Додано
- Випадаючий список вибору черги (1.1..6.2) у хедері
- Збереження вибору в localStorage, застосування при завантаженні
- Оновлення API URL/заголовка при зміні черги, мобільні стилі


## [1.12] - 2025-10-10

### Додано
- Випадаючий список вибору черги (1.1..6.2) у хедері
- Збереження вибору в localStorage, застосування при завантаженні
- Оновлення API URL/заголовка при зміні черги, мобільні стилі

## [1.11] - 2025-10-10

### Додано
- Додано банер для донату на Русоріз у футері сторінки
- Банер містить логотип Spilnota та посилання на monobank jar
- Висота банера 100px, чорний фон

## [1.10] - 2025-10-10

### Виправлено
- Виправлено алгоритм парсингу діапазонів годин з API
- Дозволено значення до 24 для діапазонів типу "22-24"
- Змінено цикл з `h <= end` на `h < end` для правильного парсингу
- Додано перевірку `h < 24` для безпеки

## [1.9] - 2025-10-10

### Додано
- Додано browser notifications при зміні графіку
- Додано запит дозволу на нотифікації при завантаженні сторінки
- Додано футер зі статусом нотифікацій (англійською)
- Додано збереження статусу нотифікацій в localStorage
- Винесено CSS стилі в окремий файл `styles.css`

## [1.8] - 2025-10-10

### Додано
- Додано Google Analytics tracking code (ID: G-E0SNV6JLNQ)

## [1.7] - 2025-10-10

### Додано
- Додано автоматичне виділення поточної години блакитною рамкою
- Додано автоматичне оновлення виділення кожну хвилину

## [1.6] - 2025-10-10

### Додано
- Додано парсер для формату `schedule` з діапазонами годин
- Підтримка формату: "02-04, 06-08, 10-13" (діапазони годин відключення)

## [1.5] - 2025-10-10

### Додано
- Додано виведення відповіді від API в консоль для дебагу

## [1.4] - 2025-10-10

### Додано
- Додано відображення версії коду на сторінці
- Інтегровано CORS proxy (corsproxy.io) для обходу обмежень

## [1.3] - 2025-10-10

### Виправлено
- Покращено обробку помилок CORS
- Додано кілька альтернативних CORS proxy сервісів

## [1.2] - 2025-10-10

### Додано
- Додано відображення версії коду на сторінці

## [1.1] - 2025-10-10

### Додано
- Додано обробку помилок CORS
- Додано використання CORS proxy для обходу обмежень

## [1.0] - 2025-10-10

### Додано
- Початкова версія проєкту
- Графік відключень електрики на 24 години
- Підтримка CORS proxy для завантаження даних
- Візуалізація стану електрики (є/відключено)

